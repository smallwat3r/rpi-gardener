{
  "name": "rpi-gardener-frontend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "format": "biome format --write src",
    "lint": "biome lint src",
    "check": "biome check src"
  },
  "dependencies": {
    "echarts": "^6.1.0",
    "preact": "^10.28.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@preact/preset-vite": "^2.10.5",
    "typescript": "^5.3.3",
    "vite": "^8.0.16"
  }
}
